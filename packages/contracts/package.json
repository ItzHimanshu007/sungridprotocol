{
    "name": "contracts",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "test": "forge test",
        "build": "forge build",
        "deploy:sepolia": "source .env && forge script script/Deploy.s.sol:DeployScript --rpc-url sepolia --broadcast --legacy"
    }
}